# See also Kener Documentation at https://kener.ing/docs

owner=agility-game
repo=kener

# Set PUBLIC_KENER_FOLDER as an environment variable. Value should be the path to a directory where kener will store its data.
# Example:
# export PUBLIC_KENER_FOLDER=/workspace/kener/static/kener
PUBLIC_KENER_FOLDER=/path/to/a/directory # REQUIRED /path/to/a/directory e.g. workspace/kener/static/kener
PORT=3000 # Defaults to 3000 if not specified
# GH_TOKEN=your-github-token # A github token to read issues and create labels. Use Doppler instead!
# API_TOKEN=some-token-set-by-you # To talk to kener apis you will need to set up a token. It uses Bearer Authorization. e.g. some-token-set-by-you See https://apidog.com/articles/how-to-generate-bearer-token/ Use Doppler instead!
API_IP=127.0.0.1 # While using API you can set this variable to accept request from a specific IP
# If you do not specify MONITOR_YAML_PATH or SITE_YAML_PATH it will take the values from /config/monitors.yaml and /config/site.yaml respectively.
MONITOR_YAML_PATH=config/monitors.yaml # /your/path/monitors.yaml
SITE_YAML_PATH=config/site.yaml # /your/path/site.yaml